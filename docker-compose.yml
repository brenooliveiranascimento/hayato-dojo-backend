services:
  todo_dev_database:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=hayato
      - POSTGRES_PASSWORD=karate_hayato
      - POSTGRES_DB=karate_db
    volumes:
      - .:/usr/postgres_database
